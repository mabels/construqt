#!/bin/bash

TYPE=$1
NAME=$2
STATE=$3

case $STATE in
  "MASTER")
    <%= vrrp.render_masters %>
    exit 0
    ;;
  "BACKUP")
    <%= vrrp.render_backups %>
    exit 0
    ;;
  *)
    echo "unknown state"
    exit 1
    ;;
esac
